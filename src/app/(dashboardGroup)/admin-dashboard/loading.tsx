import React from "react";
import Loading from "../_components/allTableSkeleton/loading";

const TableLoading = () => {
  return (
    <div>
      <Loading />
    </div>
  );
};

export default TableLoading;
